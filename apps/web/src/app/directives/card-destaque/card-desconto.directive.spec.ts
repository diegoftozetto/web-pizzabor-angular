import { CardDescontoDirective } from './card-desconto.directive';

describe('CardDescontoDirective', () => {
  it('should create an instance', () => {
    const directive = new CardDescontoDirective();
    expect(directive).toBeTruthy();
  });
});
