import { ReplaceCategoriaPipe } from './replace-categoria.pipe';

describe('ReplaceCategoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceCategoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
